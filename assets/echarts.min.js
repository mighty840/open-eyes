/* ECharts 5.x placeholder - download from https://cdn.jsdelivr.net/npm/echarts@5/dist/echarts.min.js */
/* This file must be replaced with the actual ECharts library for charts to work */
console.warn("ECharts placeholder loaded - replace with actual echarts.min.js");
window.echarts = window.echarts || {
    init: function(el) {
        return {
            setOption: function(opt) { el.innerHTML = '<p style="color:#888;padding:20px">ECharts not loaded. Replace assets/echarts.min.js with the real library.</p>'; },
            resize: function() {},
            dispose: function() {}
        };
    }
};
